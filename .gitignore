# Local source material cache.
#
# This directory may contain licensed standards, downloaded public PDFs,
# extraction notes, and other source material used to create original
# distilled project artifacts. It must stay out of git.
.source-materials/

# Local agent workflow/cache artifacts.
.context/
.memsearch/
.worktrees/

# TraceWeaver deterministic fixtures intentionally carry hidden support-surface
# examples so review can inspect and stage them like normal evidence.
!fixtures/code-traceability/audit-surface-classification/.source-materials/
!fixtures/code-traceability/audit-surface-classification/.source-materials/**
!fixtures/code-traceability/audit-surface-classification/.memsearch/
!fixtures/code-traceability/audit-surface-classification/.memsearch/**
!fixtures/code-traceability/audit-surface-classification/.worktrees/
!fixtures/code-traceability/audit-surface-classification/.worktrees/**

# macOS Finder metadata.
.DS_Store

# Beads / Dolt files (added by bd init)
.beads/
.dolt/
*.db
.beads-credential-key
